package com.app.legend.waraumusic.fragment.interfaces;

import android.support.v4.media.MediaMetadataCompat;

public interface SearchMusicClickListener {

    void click(MediaMetadataCompat metadataCompat);

}
